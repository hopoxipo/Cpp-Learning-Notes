#include "Student.h"
#include <vector>

int main()
{
    while (true)
    {
        display();
        switch()
    }
    Student stu1(1, "Alice", 89);
    Student stu2(2, "Bob", 78);
    Student stu3(3, "Cathy", 90);
    std::vector<Student> stuvec = { stu1, stu2, stu3 };
    sys(stuvec);
}
